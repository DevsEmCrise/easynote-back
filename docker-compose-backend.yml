services:
  backend:
    build: .
    container_name: easynote-backend
    ports:
      - "8082:8082"
    env_file:
      - .env
    environment:
      DB1_HOST: easynote-db
    networks:
      - easynote-net
    restart: on-failure

networks:
  easynote-net:
    external: true
